# sandbox-env-default

A customized Debian-based Node.js Docker environment pre-configured with Git, cURL, Python 3, pnpm, and custom CLI tools including `agent-browser` (with tab isolation) and `search-cli`.
